# Base URL for the real backend once it exists.
# Until then, src/api/* serves mock data automatically.
VITE_API_URL=http://localhost:5000/api

# Socket.io server for Chat / Live Tracking.
VITE_SOCKET_URL=http://localhost:5000
